{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Post</h1>

    <table class="table table-responsive table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Body</th>
                <td>{{ entity.body }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('post') }}" class="btn btn-default">Back to the list</a>

    <a href="{{ path('post_edit', { 'id': entity.id }) }}" class="btn btn-default">Edit</a>

    {{ form(delete_form) }}

{% endblock %}
