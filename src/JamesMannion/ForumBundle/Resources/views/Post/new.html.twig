{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Post creation</h1>

    <a href="{{ path('post') }}" class="btn btn-default">Back to list</a>

    <form action="{{ path('post_create', { 'id': thread.id }) }}" method="post" >
        {{ form_errors(form) }}
        {{ form_rest(form) }}
    </form>

{% endblock %}
