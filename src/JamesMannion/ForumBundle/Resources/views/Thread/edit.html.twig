{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Thread edit</h1>

    <a href="{{ path('thread') }}" class="btn btn-default">Back to List</a>

    {{ form(edit_form) }}

    {{ form(delete_form) }}
{% endblock %}
